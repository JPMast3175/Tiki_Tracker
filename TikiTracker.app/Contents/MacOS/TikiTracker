#!/bin/bash
# Tiki Tracker — macOS app launcher
# Calls the venv Python directly; no `source` needed.

PROJ="/Users/josephmast/Tiki_Tracker"
PYTHON="$PROJ/venv/bin/python"
LOG="$PROJ/launcher.log"

exec "$PYTHON" "$PROJ/run.py" >> "$LOG" 2>&1
